import { locale } from '../locale';
import { Button } from './Button';

function AlertSubscriptionButton() {
    return <Button>{locale.shared.alertSubscriptionButton}</Button>;
}

export { AlertSubscriptionButton };
