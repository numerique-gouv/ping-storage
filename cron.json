{
    "jobs": [
        {
            "command": "*/10 * * * * npm run script:checkCronMonitors",
            "size": "S"
        },
        {
            "command": "*/10 * * * * npm run script:pingAppMonitors",
            "size": "S"
        }
    ]
}
